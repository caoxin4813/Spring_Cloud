# Eureka Client 服务参数配置
eureka:
  client:
    # 注册中心地址
    service-url:
      defaultZone: http://localhost:8761/eureka/
# 应用服务端口
server:
  port: 8762
# 应用名称
spring:
  application:
    name: CaaS_Zuul_Client

